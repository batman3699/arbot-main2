[profile.default]
solc_version = "0.8.21"
optimizer = true
optimizer_runs = 200
src = "contracts"
script = "script"
test = "test"
out = "out"
libs = ["lib"]
via_ir = true

# Remappings if needed later
remappings = [
    "forge-std/=lib/forge-std/src/"
]
