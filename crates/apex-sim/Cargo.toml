[package]
name = "apex-sim"
version = "0.1.0"
edition = "2021"
description = "The simulation hierarchy: analytic screening, local EVM, and full node simulation."

[dependencies]
apex-types = { path = "../apex-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1"
alloy-primitives = "0.8"
